# Registry metadata

`registry/server.json.example` documents the intended metadata for the Workspace server.

The server is **not published** to an MCP registry in this milestone. Update and validate the document before any future publication attempt.
